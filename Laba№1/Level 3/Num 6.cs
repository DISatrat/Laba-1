using System;
using System.Collections.Generic;
using System.Text;

namespace Laba_1.Level_3
{
    class Num_6
    {
        //static void Main(string[] args)
        //{
        //    double lim = 0.0001;
        //    double i = 1;
        //    double s = 0;

        //    double y = 0;
        //    for (double x = 0.1; x <= 1; x += 0.1)
        //    {
        //        if (s <= 0.0001)
        //        {

        //            s = (Math.Pow(-1, i + 1)) * (Math.Pow(x, 2 * i + 1)) / (4 * i * i - 1) + s;

        //            y = ((1 + x * x) * Math.Atan(x)) / (2) - (x / 2);

        //            Console.WriteLine("при x=" + x + " s=" + s + " y=" + y);

        //        }
        //    }


        //}
    }
}
